#!/bin/bash
#
cd $(pwd)/../src/
javac -Xlint:unchecked -cp  .:$(pwd)/../lib/* littlelog/*.java
time java -Xmx12G  -cp .:$(pwd)/../lib/* littlelog/Main "$@"
find . -type f -path "*/*" -name "*.class" -exec rm -f {} \;

